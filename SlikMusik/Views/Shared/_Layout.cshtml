<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - SlikMusik</title>
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
</head>
<body>
    @Html.ActionLink("SlikMusik", "List", "Store", null, new { @class = "brand" })
    @Html.ActionLink("Your shopping cart", "Show", "ShoppingCart")

    <div class="container body-content">
        @RenderBody()
    </div>
</body>
</html>